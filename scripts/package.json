{
  "name": "coval-sdk-codegen-scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Scripts that bundle Coval's per-resource OpenAPI specs and run codegen for the TypeScript + Python SDKs.",
  "scripts": {
    "bundle": "node bundle-spec.mjs",
    "generate": "bash generate-sdks.sh"
  },
  "dependencies": {
    "@redocly/cli": "2.39.0",
    "yaml": "2.9.0"
  }
}
