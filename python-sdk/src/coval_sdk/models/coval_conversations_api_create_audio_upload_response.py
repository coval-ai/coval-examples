# coding: utf-8

"""
    Coval Agents API

     Manage configurations for simulations and evaluations. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@coval.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CovalConversationsAPICreateAudioUploadResponse(BaseModel):
    """
    Response for `POST /v1/audio:upload`. 
    """ # noqa: E501
    upload_id: Annotated[str, Field(strict=True)] = Field(description="Opaque upload identifier. Pass this as `upload_id` on `POST /v1/conversations:submit` after you've uploaded bytes to `upload_url`. ")
    upload_url: StrictStr = Field(description="Short-lived presigned PUT URL. PUT the audio bytes here with the matching `Content-Type` header from `content_type`. ")
    expires_at: datetime = Field(description="Absolute UTC timestamp at which the presigned URL stops being valid. The `upload_id` itself remains resolvable for 7 days from issuance, but you cannot upload bytes after this timestamp. ")
    max_size_bytes: StrictInt = Field(description="Hard upper bound on the uploaded payload size, in bytes.")
    content_type: StrictStr = Field(description="Content-Type the client must send on the PUT request. Equal to the `content_type` requested (or default `audio/wav`). ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["upload_id", "upload_url", "expires_at", "max_size_bytes", "content_type"]

    @field_validator('upload_id')
    def upload_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^upl_[0-9A-HJKMNP-TV-Z]{26}$", value):
            raise ValueError(r"must validate the regular expression /^upl_[0-9A-HJKMNP-TV-Z]{26}$/")
        return value

    @field_validator('content_type')
    def content_type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['audio/wav', 'audio/mp3']):
            raise ValueError("must be one of enum values ('audio/wav', 'audio/mp3')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CovalConversationsAPICreateAudioUploadResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CovalConversationsAPICreateAudioUploadResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "upload_id": obj.get("upload_id"),
            "upload_url": obj.get("upload_url"),
            "expires_at": obj.get("expires_at"),
            "max_size_bytes": obj.get("max_size_bytes"),
            "content_type": obj.get("content_type")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


