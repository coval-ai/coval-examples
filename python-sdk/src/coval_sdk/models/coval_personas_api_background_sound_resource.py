# coding: utf-8

"""
    Coval Agents API

     Manage configurations for simulations and evaluations. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@coval.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CovalPersonasAPIBackgroundSoundResource(BaseModel):
    """
    CovalPersonasAPIBackgroundSoundResource
    """ # noqa: E501
    id: StrictStr = Field(description="Built-in sound id or custom asset id.")
    value: StrictStr = Field(description="Value to store in persona.background_sound. Custom sounds use custom:<background_sound_id>.")
    source: StrictStr
    display_name: StrictStr
    status: StrictStr
    preview_url: Optional[StrictStr] = Field(default=None, description="Short-lived URL for preview playback. Built-in previews may use public URLs.")
    preview_url_expires_at: Optional[datetime] = None
    default_volume: Union[Annotated[float, Field(strict=True, ge=0)], Annotated[int, Field(strict=True, ge=0)]]
    content_type: Optional[StrictStr] = None
    original_filename: Optional[StrictStr] = None
    acoustic_source_type: Optional[StrictStr] = Field(default=None, description="Acoustic rendering behavior for this sound. - ambient: mixed like room ambience. - point_source: rendered from a specific location when a situate_speaker preset is set; otherwise mixed like ambient. ")
    metadata: Optional[Dict[str, Any]] = None
    created_at: Optional[datetime] = None
    last_updated_at: Optional[datetime] = None
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["id", "value", "source", "display_name", "status", "preview_url", "preview_url_expires_at", "default_volume", "content_type", "original_filename", "acoustic_source_type", "metadata", "created_at", "last_updated_at"]

    @field_validator('source')
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['built_in', 'custom']):
            raise ValueError("must be one of enum values ('built_in', 'custom')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['pending_upload', 'active', 'archived']):
            raise ValueError("must be one of enum values ('pending_upload', 'active', 'archived')")
        return value

    @field_validator('acoustic_source_type')
    def acoustic_source_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['ambient', 'point_source']):
            raise ValueError("must be one of enum values ('ambient', 'point_source')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CovalPersonasAPIBackgroundSoundResource from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if preview_url (nullable) is None
        # and model_fields_set contains the field
        if self.preview_url is None and "preview_url" in self.model_fields_set:
            _dict['preview_url'] = None

        # set to None if preview_url_expires_at (nullable) is None
        # and model_fields_set contains the field
        if self.preview_url_expires_at is None and "preview_url_expires_at" in self.model_fields_set:
            _dict['preview_url_expires_at'] = None

        # set to None if content_type (nullable) is None
        # and model_fields_set contains the field
        if self.content_type is None and "content_type" in self.model_fields_set:
            _dict['content_type'] = None

        # set to None if original_filename (nullable) is None
        # and model_fields_set contains the field
        if self.original_filename is None and "original_filename" in self.model_fields_set:
            _dict['original_filename'] = None

        # set to None if acoustic_source_type (nullable) is None
        # and model_fields_set contains the field
        if self.acoustic_source_type is None and "acoustic_source_type" in self.model_fields_set:
            _dict['acoustic_source_type'] = None

        # set to None if created_at (nullable) is None
        # and model_fields_set contains the field
        if self.created_at is None and "created_at" in self.model_fields_set:
            _dict['created_at'] = None

        # set to None if last_updated_at (nullable) is None
        # and model_fields_set contains the field
        if self.last_updated_at is None and "last_updated_at" in self.model_fields_set:
            _dict['last_updated_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CovalPersonasAPIBackgroundSoundResource from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "id": obj.get("id"),
            "value": obj.get("value"),
            "source": obj.get("source"),
            "display_name": obj.get("display_name"),
            "status": obj.get("status"),
            "preview_url": obj.get("preview_url"),
            "preview_url_expires_at": obj.get("preview_url_expires_at"),
            "default_volume": obj.get("default_volume"),
            "content_type": obj.get("content_type"),
            "original_filename": obj.get("original_filename"),
            "acoustic_source_type": obj.get("acoustic_source_type"),
            "metadata": obj.get("metadata"),
            "created_at": obj.get("created_at"),
            "last_updated_at": obj.get("last_updated_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


