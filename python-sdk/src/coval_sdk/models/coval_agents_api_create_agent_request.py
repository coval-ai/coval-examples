# coding: utf-8

"""
    Coval Agents API

     Manage configurations for simulations and evaluations. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@coval.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from coval_sdk.models.coval_agents_api_simulator_type import CovalAgentsAPISimulatorType
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CovalAgentsAPICreateAgentRequest(BaseModel):
    """
    CovalAgentsAPICreateAgentRequest
    """ # noqa: E501
    display_name: Annotated[str, Field(min_length=1, strict=True, max_length=200)] = Field(description="Human-readable agent name")
    model_type: CovalAgentsAPISimulatorType
    phone_number: Optional[Annotated[str, Field(strict=True, max_length=200)]] = Field(default=None, description="Phone number in E.164 format (e.g., +12345678901) or SIP address (e.g., sip:user@domain.com).  **Required for:** - MODEL_TYPE_VOICE agents (E.164 or SIP) - MODEL_TYPE_SMS agents (E.164 only)  Optional for other agent types. ")
    endpoint: Optional[Annotated[str, Field(strict=True, max_length=200)]] = Field(default=None, description="Webhook endpoint URL that Coval will POST to.  **Required for MODEL_TYPE_OUTBOUND_VOICE agents.** Must be a valid HTTP/HTTPS URL. ")
    prompt: Optional[Annotated[str, Field(strict=True, max_length=50000)]] = Field(default=None, description="Agent instructions/system prompt")
    metadata: Optional[Dict[str, Any]] = Field(default=None, description="Simulator-specific configuration (JSONB, max 10MB).  **For CHAT agents:**  *Required:* - `chat_endpoint` - Chat endpoint URL (validated, no private IPs/localhost)  *Authentication:* - `authorization_header` - Auth header (Bearer, X-API-Key, etc.)  *Initialization:* - `initialization_endpoint` - Init endpoint called before chat (validated URL) - `initialization_payload` - JSON payload for init request (max 16KB)  *Custom Data:* - `custom_data` - Organization/agent-level data (max 32KB JSON string) - `custom_persona_data` - Persona-specific data (max 16KB JSON string)  *Headers & Format:* - `custom_headers` - Additional headers with template variables (max 16KB JSON string) - `response_format` - \"chat_completions\" (default) or \"responses\"  *Request/Response Processing:* - `input_template` - Custom JSON template for requests (validated JSON) - `payload_wrapper` - Wrapper field name (e.g., \"data\", \"request\") - `response_message_path` - Dot notation path to extract message - `strip_message_timestamps` - Remove timestamp fields (boolean, default: false)  *Tool Calls:* - `tool_call_extraction_config` - Configuration for extracting tool calls from custom responses  *Dynamic Configuration:* - `persona_id` - Reference persona for parameter substitution (22-char ShortUUID)  *Template Variables:* `{{sessionId}}`, `{{simulation_output_id}}`, `{{init_response.path}}`, `{{persona.field}}`, `{{messages}}`, `{{latest_message}}`, `{{custom_data.field}}`  **For CHAT_A2A agents:**  *Required:* - `chat_endpoint` - A2A JSON-RPC endpoint URL (validated, no private IPs/localhost)  *Initialization (optional):* - `initialization_endpoint` - Auth endpoint called once per simulation; if response contains `access_token`, it is used as `Authorization: Bearer <token>` for chat requests - `initialization_payload` - Body sent to initialization endpoint (JSON string) - `initialization_content_type` - `application/x-www-form-urlencoded` (default) or `application/json` - `authorization_header` - Static auth value, used when no initialization endpoint is configured  *Headers:* - `custom_headers` - Additional headers sent on every chat request (JSON string)  *Response Parsing:* - `response_message_path` - Dot-notation path to the agent's reply text (default: `result.artifacts.0.parts.0.text`) - `response_state_extraction` - JSON paths to the `contextId` and `taskId` echoed back on each turn (default: `{\"contextId\": \"result.contextId\", \"taskId\": \"result.id\"}`). Keys must be `contextId` and `taskId`; only the paths are configurable.  *End-of-Conversation:* - `end_state_path` - Dot-notation path to the conversation status field (default: `result.status.state`) - `end_state_values` - List of status values that terminate the conversation (default: `[\"completed\", \"failed\"]`)  *Custom Data:* - `custom_data` - Arbitrary JSON object merged into `params.metadata` on every chat request  **For OUTBOUND_VOICE agents:** - `trigger_call_headers` - JSON string with HTTP headers - `trigger_call_payload` - JSON string with base payload - `phone_number_key` - Field name for phone number (default: \"phone_number\")  **For WEBSOCKET agents:**  *Required:* - `endpoint` - WebSocket endpoint URL (must be wss://, validated) - `initialization_json` - JSON string payload sent on connection  *Authentication (optional):* - `authorization_header` - Auth header sent during WebSocket handshake. Supports:   - `\"Bearer <token>\"` → sent as `Authorization: Bearer <token>`   - `\"X-API-Key <key>\"` → sent as `X-API-Key: <key>` - `custom_headers` - JSON string of additional HTTP headers for WebSocket handshake ")
    workflows: Optional[Dict[str, Any]] = Field(default=None, description="Workflow configuration (JSONB, max 10MB)")
    metric_ids: Optional[List[Annotated[str, Field(strict=True)]]] = Field(default=None, description="Associated metric IDs (22-char IDs)")
    test_set_ids: Optional[List[Annotated[str, Field(strict=True)]]] = Field(default=None, description="Associated test set IDs (22-char IDs)")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["display_name", "model_type", "phone_number", "endpoint", "prompt", "metadata", "workflows", "metric_ids", "test_set_ids"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CovalAgentsAPICreateAgentRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if phone_number (nullable) is None
        # and model_fields_set contains the field
        if self.phone_number is None and "phone_number" in self.model_fields_set:
            _dict['phone_number'] = None

        # set to None if endpoint (nullable) is None
        # and model_fields_set contains the field
        if self.endpoint is None and "endpoint" in self.model_fields_set:
            _dict['endpoint'] = None

        # set to None if prompt (nullable) is None
        # and model_fields_set contains the field
        if self.prompt is None and "prompt" in self.model_fields_set:
            _dict['prompt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CovalAgentsAPICreateAgentRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "display_name": obj.get("display_name"),
            "model_type": obj.get("model_type"),
            "phone_number": obj.get("phone_number"),
            "endpoint": obj.get("endpoint"),
            "prompt": obj.get("prompt"),
            "metadata": obj.get("metadata"),
            "workflows": obj.get("workflows"),
            "metric_ids": obj.get("metric_ids"),
            "test_set_ids": obj.get("test_set_ids")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


