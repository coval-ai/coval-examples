# coding: utf-8

"""
    Coval Agents API

     Manage configurations for simulations and evaluations. 

    The version of the OpenAPI document: 1.0.0
    Contact: support@coval.dev
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from coval_sdk.models.coval_conversations_api_transcript_message import CovalConversationsAPITranscriptMessage
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CovalConversationsAPISubmitConversationRequest(BaseModel):
    """
    Request to submit a conversation for monitoring evaluation.  **Requirements:** - At least one of: `transcript`, `audio_url`, or `upload_id` must be provided - `audio_url` and `upload_id` are mutually exclusive  **Payload Size Limit:** The combined queued payload is subject to a 256 KB limit (including large `metadata` values); oversized payloads return 413 `PAYLOAD_TOO_LARGE`.  **Best Practices:** - Provide both transcript and audio_url (or upload_id) when available (enables audio metrics) - **Include `start_time` and `end_time` in transcript messages when submitting audio_url** to avoid automatic retranscription - Include `external_conversation_id` for correlation with external systems - Add metadata for conditional metrics and analytics  **Audio + Transcript Behavior:** - If you submit both `transcript` and `audio_url`:   - **WITH timing** (`start_time`/`end_time` on each message): Transcript is used as-is, audio metrics computed from timing   - **WITHOUT timing**: Audio is automatically transcribed and diarized, provided transcript is ignored for metrics 
    """ # noqa: E501
    transcript: Optional[Annotated[List[CovalConversationsAPITranscriptMessage], Field(min_length=1)]] = Field(default=None, description="Conversation transcript as array of messages")
    audio_url: Optional[StrictStr] = Field(default=None, description="Presigned URL to audio file from any cloud provider.  **Audio Validation Requirements:** - **Formats**: WAV or MP3 ONLY (detected via magic bytes, not file extension) - **Channels**: Stereo (recommended) or mono. Stereo assigns speaker roles deterministically from channel position (channel 0 = agent, channel 1 = user); mono infers roles by classifying transcript content, which is typically accurate but less reliable than channel-based mapping. - **Duration**: 5 seconds minimum, 1 hour (3600 seconds) maximum - **File Size**: 200 MB maximum - **Validation**: Audio is validated BEFORE processing begins - **Rejected Formats**: MP4, M4A, FLAC, OGG, AAC, etc.  **Supported URL Formats:** - AWS S3: https://bucket.s3.amazonaws.com/... - GCP Cloud Storage: https://storage.googleapis.com/... - Azure Blob Storage: https://account.blob.core.windows.net/... - Any Public or Presigned HTTPS URL  **Validation Errors** (400 INVALID_ARGUMENT): - \"Audio file is empty.\" - \"Unsupported audio format: {format}. Only WAV and MP3 files are supported.\" - \"Audio too short: {X} seconds. Minimum duration is 5 seconds.\" - \"Audio too long: {X} minutes. Maximum duration is 60 minutes.\" - \"Audio file too large: {X} MB. Maximum size is 200 MB.\" - \"Invalid or corrupt audio file. Unable to detect audio format. Supported formats: WAV and MP3.\" - \"Invalid or corrupt audio file. Unable to read audio metadata.\" - \"Audio URL not found (404)\" or \"Failed to retrieve audio from URL\" ")
    upload_id: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Reference to a previously issued direct upload from `POST /v1/audio:upload`.  Use this when you've uploaded audio bytes directly via a Coval-issued presigned PUT URL. The Coval backend resolves the staged upload, validates it, and promotes it into managed recordings storage.  **Mutually exclusive** with `audio` and `audio_url` — provide exactly one audio source.  **Validation Errors:** - 404 NOT_FOUND: `upload_id` does not exist, has expired, has already been consumed,   or belongs to a different organization (404 is returned in all four cases to   avoid leaking ID existence across organizations). - 400 INVALID_ARGUMENT: Mutually exclusive audio sources provided. ")
    metrics: Optional[List[Annotated[str, Field(min_length=22, strict=True, max_length=26)]]] = Field(default=None, description="List of metric IDs to evaluate (22–26 character IDs).  If not provided, uses your default metrics configured in the Coval dashboard. ")
    metadata: Optional[Dict[str, Any]] = Field(default=None, description="Custom metadata for conditional metrics and tracking.  Used for: - Triggering conditional metrics - Filtering conversations - Custom analytics and reporting ")
    tags: Optional[Annotated[List[Annotated[str, Field(min_length=1, strict=True, max_length=200)]], Field(max_length=20)]] = Field(default=None, description="Optional tags to apply to this conversation for filtering and organization.  Tags are normalized case-insensitively and stored on the monitoring conversation's backing run. Use them to group uploaded conversations by product, workflow, customer segment, or other operational slices. ")
    external_conversation_id: Optional[StrictStr] = Field(default=None, description="External conversation ID from your system.  Stored as `conversation_id` in `customer_metadata` JSONField. Can be used to correlate with external systems (Twilio, Vonage, etc). ")
    occurred_at: Optional[datetime] = Field(default=None, description="When the conversation actually occurred (ISO 8601)")
    agent_id: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Agent resource ID (22-character ShortUUID) to associate with this conversation.  Must reference an existing agent within your organization. The agent is validated at submission time: - **400 INVALID_ARGUMENT**: Malformed ID (wrong length or characters). - **404 NOT_FOUND**: Agent does not exist or belongs to a different organization. ")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["transcript", "audio_url", "upload_id", "metrics", "metadata", "tags", "external_conversation_id", "occurred_at", "agent_id"]

    @field_validator('upload_id')
    def upload_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^upl_[0-9A-HJKMNP-TV-Z]{26}$", value):
            raise ValueError(r"must validate the regular expression /^upl_[0-9A-HJKMNP-TV-Z]{26}$/")
        return value

    @field_validator('agent_id')
    def agent_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz]{22}$", value):
            raise ValueError(r"must validate the regular expression /^[23456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz]{22}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CovalConversationsAPISubmitConversationRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in transcript (list)
        _items = []
        if self.transcript:
            for _item_transcript in self.transcript:
                if _item_transcript:
                    _items.append(_item_transcript.to_dict())
            _dict['transcript'] = _items
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        # set to None if agent_id (nullable) is None
        # and model_fields_set contains the field
        if self.agent_id is None and "agent_id" in self.model_fields_set:
            _dict['agent_id'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CovalConversationsAPISubmitConversationRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "transcript": [CovalConversationsAPITranscriptMessage.from_dict(_item) for _item in obj["transcript"]] if obj.get("transcript") is not None else None,
            "audio_url": obj.get("audio_url"),
            "upload_id": obj.get("upload_id"),
            "metrics": obj.get("metrics"),
            "metadata": obj.get("metadata"),
            "tags": obj.get("tags"),
            "external_conversation_id": obj.get("external_conversation_id"),
            "occurred_at": obj.get("occurred_at"),
            "agent_id": obj.get("agent_id")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


